/**
 * Compatibility stub for the 'server-only' package.
 *
 * In Next.js, importing 'server-only' throws an error if imported
 * from a client component. In our Vite SPA, everything runs on the
 * client, so this is a no-op.
 */
