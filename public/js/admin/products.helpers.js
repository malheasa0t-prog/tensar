/**
 * TechZone Admin — Products Helpers (Rebuilt)
 * Product utilities now embedded in products.js.
 */
(function () {
    'use strict';
    /* Product helpers are now embedded in products.js for simplicity. */
})();
