/**
 * TechZone Admin — Settings Promo Banners (Rebuilt)
 * Promo banners logic now managed through settings.js.
 */
(function () {
    'use strict';
    /* Promo banners are managed within settings.js for simplicity. */
})();
