/**
 * TechZone Admin — Customers Helpers (Rebuilt)
 * Shared utilities for customer module.
 */
(function () {
    'use strict';
    /* Customer helpers are now embedded in customers.js for simplicity. */
})();
