/**
 * TechZone Admin — Notifications Helpers (Rebuilt)
 * Shared utilities for notifications module.
 */
(function () {
    'use strict';
    /* Notification helpers are now embedded in notifications.js for simplicity. */
})();
