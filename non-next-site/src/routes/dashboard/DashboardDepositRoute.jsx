import DepositRequestPanel from "../../components/DepositRequestPanel.jsx";

/**
 * Renders the wallet deposit dashboard route in the non-Next copy.
 *
 * @returns {JSX.Element}
 */
export default function DashboardDepositRoute() {
  return <DepositRequestPanel />;
}
